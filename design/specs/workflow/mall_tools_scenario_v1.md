# 小场景实例：功率测量任务工具串联 v1.0

> **声明**: 本文档内容通过引导AI自动生成，仅供设计参考与学术交流。

> **场景**：测量功率 vs 频率，设置脉宽，采集功率计数据
> **复杂因素**：被测件开启后存在严重滞后，功率计可能有故障，信号源可能有故障
> **目标**：展示全生命周期工具如何协同工作

---

## 一、场景背景

### 1.1 任务定义

| 要素 | 内容 |
|------|------|
| **任务名称** | TR模块功率特性测量 |
| **测量对象** | 微波TR组件 |
| **频率范围** | 1 GHz – 6 GHz |
| **步进** | 100 MHz |
| **脉宽设置** | 10 μs |
| **被测件特性** | 开启后存在2秒建立时间 |
| **风险因素** | 功率计偶发异常码-999（饱和），信号源偶发失锁 |

### 1.2 生产者阵容

| 角色 | 生产者ID | 功能 |
|------|---------|------|
| 信号源 | `sig_gen_01` | 产生激励信号，频率/功率可控 |
| 功率计 | `pwr_meter_01` | 测量输出功率 |
| 示波器 | `scope_01` | 监测脉冲波形质量 |

---

## 二、阶段一：任务发起

### 2.1 蓝图编辑器

消费者输入：
```json
{
  "input": {
    "signal_source": {
      "frequency_range": ["1GHz", "6GHz"],
      "step": "100MHz",
      "pulse_width": "10us",
      "excitation_power": "-10dBm"
    }
  },
  "output": {
    "power_meter": {
      "measurement": "peak_power",
      "unit": "dBm",
      "expected_range": [-50, 10]
    },
    "scope": {
      "measurement": "pulse_shape",
      "sample_rate": "1GSa/s"
    }
  },
  "constraints": {
    "max_duration": 600,
    "settling_time": 2,
    "retry_on_error": true,
    "max_retries": 3
  }
}
```

### 2.2 生产者匹配探针

探针扫描结果：
```
候选生产者：
1. sig_gen_01 — 匹配度98% — 历史成功率99.2% — 当前负载1/4
2. pwr_meter_01 — 匹配度95% — 历史成功率97.5% — ⚠️ 近10次任务有2次异常码
3. scope_01 — 匹配度100% — 历史成功率100% — 当前空闲

风险提示：pwr_meter_01 存在偶发饱和问题，建议增加自检步骤。
```

### 2.3 代价评估计算器

估算结果：
```
基础执行代价：51频点 × (测量时间 + 建立时间) ≈ 51 × (0.5s + 2s) = 127.5s
IO传输代价：数据量约 51 × 1KB = 51KB，传输时间 < 1s
生产者协同代价：三方同步开销 ≈ 10s
风险缓冲代价：pwr_meter_01 异常风险 +20s
─────────────────────────────────────────────
预估总时间：乐观 120s / 最可能 160s / 悲观 240s
契约最大时长：600s（充足）
```

### 2.4 契约生成器

生成契约摘要：
```
契约ID: ctr-20260512-001
消费者: user_42
生产者组: [sig_gen_01, pwr_meter_01, scope_01]
执行计划: 顺序扫描，每频点建立2s → 测量0.5s → 数据校验
约束: 最大600s，异常重试3次
批准方: 消费者 + 商场
```

---

## 三、阶段二：任务批准

### 3.1 契约可视化面板

消费者看到：
```
┌─────────────────────────────────────┐
│  任务：TR模块功率特性测量            │
│  预计耗时：约2分40秒（最长10分钟）    │
│                                     │
│  生产者：                            │
│  ✓ 信号源 sig_gen_01 (稳定性★★★★★)  │
│  ⚠ 功率计 pwr_meter_01 (稳定性★★★☆☆)│
│  ✓ 示波器 scope_01 (稳定性★★★★★)    │
│                                     │
│  ⚠ 风险提示：功率计偶发饱和，已配置  │
│    自动重试与激励调节预案。           │
│                                     │
│  [确认并签名]  [修改蓝图]  [取消]    │
└─────────────────────────────────────┘
```

### 3.2 双向确认签名

消费者点击"确认" → 签名 → 商场验证 → 商场签名 → 契约冻结。

### 3.3 沙箱预演

预演结果：通过。
- 验证三方数据格式兼容
- 验证2秒建立时间不会导致死锁
- 验证异常重试路径可行

---

## 四、阶段三：任务执行

### 4.1 实时状态监控台

```
┌────────────────────────────────────────────┐
│ 任务进度: 23/51 频点 (45%)  已用时间: 78s   │
│                                            │
│  sig_gen_01  🟢 在线  频率: 3.2GHz  锁定✓   │
│  pwr_meter_01 🟡 警告  最后读数: -999 ⚠️    │
│  scope_01    🟢 在线  波形正常              │
│                                            │
│  当前频点: 3.2GHz  功率: 异常  脉宽: 10us   │
└────────────────────────────────────────────┘
```

### 4.2 异常告警器触发

**WARNING级别告警**：
```
⚠️ 功率计异常告警
─────────────────────────────
频点: 3.2GHz
异常码: -999 (饱和)
时间: 2026-05-12T17:15:32Z
─────────────────────────────
建议操作:
1. 降低激励功率 -10dB，重新测量
2. 跳过该频点，继续后续测量
3. 暂停任务，检查功率计
─────────────────────────────
[执行建议1]  [执行建议2]  [执行建议3]
```

### 4.3 决策引擎介入

消费者预设规则触发：
```
规则"功率饱和保护"匹配：
条件: pwr_meter.status == -999
建议: 降低激励-10dB
─────────────────────────────
消费者点击: [执行建议1]
```

### 4.4 参数动态调节器执行

```
消费者UI: 点击"降低激励-10dB"
    ↓
Worker验证: 新激励功率-20dBm ∈ [-30, 0]dBm ✓
    ↓
签名并加密传输
    ↓
sig_gen_01确认: 收到调节指令，当前帧完成后生效
    ↓
下一频点(3.2GHz重测): 激励功率-20dBm
    ↓
pwr_meter_01读数: -12.5dBm ✓ 正常
    ↓
任务继续，告警解除
```

### 4.5 IO流检视器

```
数据流拓扑:
sig_gen_01 ──[3.2MB/s]──→ SHM ──[2.8MB/s]──→ 消费者UI
                ↑
         pwr_meter_01
         (当前阻塞: 0ms)

延迟分析:
生产者生成 → SHM写入: 2ms
SHM读取 → UI呈现: 5ms
端到端总延迟: 7ms ✓ (< 10ms契约要求)
```

---

## 五、阶段四：任务监督

### 5.1 质量抽检器

抽检结果：
```
已抽检: 12/23 频点
通过率: 11/12 (91.7%)
失败样本:
  频点3.2GHz (首次测量) — 异常码-999 — 已重测通过
趋势图: ████████████████████░░░░░░░░░░ 91.7%
```

### 5.2 进度推演仪

```
已完成: 23频点 / 总51频点
当前平均速率: 3.4秒/频点
预估剩余时间: (51-23) × 3.4 = 95.2秒
预估总时间: 78 + 95.2 = 173.2秒
契约上限: 600秒
风险等级: 🟢 安全 (预计用时 < 30%上限)
```

### 5.3 交易审计日志（片段）

```
17:15:32.000  seq=1024  pwr_meter_01  异常码:-999  签名:...  完整性:FAIL
17:15:35.500  seq=1025  sig_gen_01    调节:-10dBm   签名:...  完整性:PASS
17:15:38.200  seq=1026  pwr_meter_01  读数:-12.5dBm 签名:...  完整性:PASS
17:15:38.205  seq=1027  consumer_42   确认:继续      签名:...  完整性:PASS
```

---

## 六、阶段五：任务完结

### 6.1 成果验收面板

```
┌─────────────────────────────────────────────┐
│ 任务完成！                                   │
│ 总频点: 51  成功: 50  异常: 1(已恢复)          │
│ 总用时: 185秒  (预估160秒，偏差+15.6%)        │
│                                             │
│ 数据质量:                                    │
│  功率测量: 50点有效，范围 -45.2 ~ +8.7 dBm    │
│  波形质量: 51点全部通过脉宽校验               │
│                                             │
│ ⚠️ 注意: 频点3.2GHz首次测量异常，已重测通过   │
│                                             │
│ [完全接受]  [部分接受]  [驳回]               │
└─────────────────────────────────────────────┘
```

消费者点击：**完全接受**

### 6.2 数据归档打包器

归档包生成：
```
task_archive_ctr-20260512-001.tar (2.3MB)
├── manifest.json
├── data/
│   ├── raw/power_meter_51pts.csv
│   ├── raw/scope_waveforms.bin
│   └── processed/power_vs_freq.png
├── logs/
│   ├── audit_log/  (1024条记录)
│   ├── control_log/ (3次调节记录)
│   └── alarm_log/ (1次WARNING)
├── contracts/
│   └── signed_contract.json
└── signatures/ (5个签名文件)
```

### 6.3 生产者评价器

消费者评价：
```
sig_gen_01:    ★★★★★  "频率切换稳定，失锁零次"
pwr_meter_01:  ★★★☆☆  "偶发饱和，但重试机制有效，建议校准"
scope_01:      ★★★★★  "波形捕获完美"
```

### 6.4 蓝图进化建议器

建议：
```
1. 参数优化: "建议初始激励设为-20dBm，避免功率计饱和"
2. 流程优化: "建议在正式测量前增加功率计自检步骤"
3. 生产者推荐: "scope_01表现优异，建议加入偏好列表"
```

消费者采纳：建议1、建议2。

---

## 七、工具串联总结图

```
[发起]  蓝图编辑器 → 匹配探针 → 代价计算器 → 契约生成器
           ↓
[批准]  可视化面板 → 双向签名 → 沙箱预演
           ↓
[执行]  监控台 → 告警器(3.2GHz异常) → 决策引擎 → 调节器(-10dB) → IO检视器
           ↓
[监督]  抽检器(91.7%) → 推演仪(173s预估) → 审计日志(1024条)
           ↓
[完结]  验收面板 → 归档打包 → 评价器 → 进化建议器(采纳2条)
```

---

**审核状态**：待审核
**版本**：v1.0
**日期**：2026-05-12
